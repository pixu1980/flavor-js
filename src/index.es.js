/*global Window*/

// const flavorInitialized = (!!window && window instanceof Window && !!window.Flavor && !!window.ƒ);

// const flavor = flavorInitialized ? window.Flavor : new Flavor();

// window.Flavor = Flavor;
// window.ƒ = Flavor;
import Flavor from './Flavor';

export default Flavor;
